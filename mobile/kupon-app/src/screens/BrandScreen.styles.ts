// src/screens/BrandScreen.styles.ts
// Styles are now co-located in BrandScreen.tsx for simplicity with this specific screen.
// This file can be removed if not needed for other shared styles related to Brands.

// If you prefer to keep styles separate, you can move the StyleSheet.create({...})
// from BrandScreen.tsx to this file and import it back. For example:

/*
import { StyleSheet } from 'react-native';
import colors from '../constants/colors';

export const styles = StyleSheet.create({
  centered: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: colors.background,
  },
  container: {
    flex: 1,
    backgroundColor: colors.background,
  },
  headerContainer: {
    alignItems: 'center',
    paddingVertical: 20,
    paddingHorizontal: 15,
    borderBottomWidth: 1,
    borderBottomColor: colors.border,
    backgroundColor: colors.card,
    marginBottom: 10,
  },
  brandLogo: {
    width: 120,
    height: 60,
    marginBottom: 15,
  },
  brandName: {
    fontSize: 28,
    fontWeight: 'bold',
    color: colors.primary,
    marginBottom: 8,
    textAlign: 'center',
  },
  brandDescription: {
    fontSize: 15,
    color: colors.textSecondary,
    textAlign: 'center',
    marginBottom: 12,
    paddingHorizontal: 10,
  },
  websiteLink: {
    fontSize: 16,
    color: colors.secondary,
    fontWeight: '600',
    marginBottom: 15,
    textDecorationLine: 'underline',
  },
  couponsHeader: {
    fontSize: 20,
    fontWeight: 'bold',
    color: colors.text,
    marginTop: 20,
    marginBottom: 10,
    alignSelf: 'flex-start',
    paddingHorizontal: 5,
  },
  listContentContainer: {
    paddingHorizontal: 5,
  },
  noCouponsText: {
    textAlign: 'center',
    marginTop: 20,
    fontSize: 16,
    color: colors.textSecondary,
  },
  errorText: {
    fontSize: 16,
    color: 'red',
    textAlign: 'center',
  },
});
*/

// All styles have been moved to BrandScreen.tsx for co-location.
// This file (BrandScreen.styles.ts) is no longer in use for BrandScreen specific styles
// and can be safely removed from the project if it serves no other purpose.
